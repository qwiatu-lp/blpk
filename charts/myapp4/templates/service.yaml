apiVersion: v1
kind: Service
metadata:
  name: {{ include "myapp4.fullname" . }}
  labels:
    {{- include "myapp4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myapp4.selectorLabels" . | nindent 4 }}
